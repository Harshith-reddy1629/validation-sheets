// import {cva} from 'class-variance-authority'

// const stylesB = cva(["bg-neutral"])

function Button({children}) {
  return (
    <button type="button"  >{children}</button>
  )
}

export default Button
